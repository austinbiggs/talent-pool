type Candidate {
  id: Int!
  name: CandidateName!
  education: Education!
}

type CandidateName {
  first: String!
  last: String!
}

type Education {
  id: Int!
  school: School!
  major: Major!
}

type School {
  id: Int!
  name: String!
}

type Major {
  id: Int!
  name: String!
}
